<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MakersMarkt.Pages.Product.OverviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakersMarkt.Pages.Product"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MakersMarkt.Data"
    mc:Ignorable="d">

    <StackPanel>
        <Button Click="Button_Click_Create">
            Post je Item
        </Button>
        <ListView x:Name="ProductListView"
       IsItemClickEnabled="True"
       SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Product">
                    <StackPanel>
                        <TextBlock Text="{x:Bind Name}" />
                        <TextBlock Text="{x:Bind Price}" />
                        <TextBlock Text="{x:Bind OrderedText }"/>
                        <Button Click="Button_Click_Open">Bekijk Order</Button>
                        <Button Click="Button_Click_Edit" >Edit</Button>
                        <Button Click="Button_Click_Delete" >Delete</Button>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
