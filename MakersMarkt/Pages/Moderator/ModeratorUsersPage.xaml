<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MakersMarkt.Pages.Moderator.ModeratorUsersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakersMarkt.Pages.Moderator"
    xmlns:data="using:MakersMarkt.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Gebruikers Beheren" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>
        
        <ListView x:Name="UsersListView" Grid.Row="1" IsItemClickEnabled="True" ItemClick="UsersListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:User">
                    <Grid Padding="12" Margin="0,4" CornerRadius="4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                        <StackPanel>
                            <TextBlock Text="{x:Bind Username}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{x:Bind Description}" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                            <TextBlock Text="{x:Bind Role}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
