<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MakersMarkt.Pages.Moderator.ModeratorProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakersMarkt.Pages.Moderator"
    xmlns:data="using:MakersMarkt.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Producten Beheren" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24"/>
        
        <ListView x:Name="ProductsListView" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Product">
                    <Grid Padding="12" Margin="0,4" CornerRadius="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{x:Bind Description}" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                            <TextBlock>
                                <Run Text="Prijs: €" />
                                <Run Text="{x:Bind Price}" />
                            </TextBlock>
                        </StackPanel>
                        
                        <Button Grid.Column="2" Content="Verwijderen" Click="DeleteProduct_Click" Tag="{x:Bind Id}" Background="Red" Foreground="White" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
